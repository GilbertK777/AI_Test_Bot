# .env - 환경 변수 설정 파일
# 이 파일에 민감한 정보(API 키 등)나 실행 환경에 따라 달라지는 설정 값을 저장합니다.
# .gitignore에 이 파일이 포함되어 있어, Git 저장소에는 올라가지 않습니다.

# --- API 및 계정 관련 설정 ---
# 실제 키가 아니어도 괜찮습니다. 프로그램 시작 시 변수가 존재하는지만 확인하기 때문입니다.
BINANCE_API_KEY="YOUR_API_KEY"
BINANCE_API_SECRET="YOUR_API_SECRET"
TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"
TELEGRAM_CHAT_ID="YOUR_TELEGRAM_CHAT_ID"

# --- 거래 관련 설정 ---
EXCHANGE="BYBIT" # 'BINANCE' 또는 'BYBIT'
SYMBOL="BTC/USDT"  # 거래할 코인 심볼
LEVERAGE=5         # 레버리지
TEST_MODE="true"   # 'true'는 모의 투자, 'false'는 실전 투자
INIT_BALANCE=10000 # 모의 투자 시 시작 자본금 (USDT)
POSITION_MARGIN=20 # 1회 거래에 사용할 증거금 (0으로 설정 시 아래 POSITION_SIZE 사용)
POSITION_SIZE=0    # 1회 거래에 사용할 수량 (POSITION_MARGIN이 0일 때만 사용)

# --- 전략 관련 설정 ---
PROB_BUY_TH=0.60
PROB_SELL_TH=0.45
PROB_SHORT_TH=0.40
STOP_LOSS_PCT=0.03 # 3%
TP_PCT=0.05        # 5%

# --- 실행 주기 설정 ---
SLEEP_SEC=60       # 60초마다 메인 루프 실행
TRAIN_HR=24        # 24시간마다 모델 재학습
